# 📚 Simple RAG System

Простая система для поиска и генерации ответов на основе извлеченной информации (Retrieval-Augmented Generation).

## 📁 Структура проекта

```
project/
├── 01_indexing.py          # Индексатор документов
├── 02_retriever.py         # Ретривер и генератор
├── data/                   # Папка с документами (создается пользователем)
│   ├── document1.txt
│   ├── document2.pdf
│   └── document3.docx
├── model_cache/            # Кэш моделей (создается автоматически)
├── rag_index.faiss         # Векторный индекс (создается после индексации)
├── rag_index_data.pkl      # Данные чанков и метаданных
└── README.md
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install sentence-transformers faiss-cpu langchain pypdf docx2txt transformers torch
```

### 2. Подготовка документов

Поместите ваши документы в папку `data/`. Поддерживаемые форматы:
- 📄 `.txt` - текстовые файлы
- 📊 `.pdf` - PDF документы
- 📝 `.docx` - документы Word

### 3. Индексация документов

```python
# Запустите 01_indexing.py
python 01_indexing.py
```

Или используйте в коде:

```python
from 01_indexing import SimpleRAGIndexer

indexer = SimpleRAGIndexer()
stats = indexer.build('путь/к/вашей/папке/с/документами')
print(f"Проиндексировано: {stats['num_documents']} документов, {stats['num_chunks']} чанков")
```

### 4. Поиск и генерация ответов

```python
# Запустите 02_retriever.py
python 02_retriever.py
```

Или используйте в коде:

```python
from 02_retriever import SimpleRAGRetriever, RAGGenerator

# Инициализация ретривера
retriever = SimpleRAGRetriever(index_path='rag_index')

# Поиск релевантных фрагментов
query = "ваш запрос"
results = retriever.retrieve(query, top_k=5)

# Генерация ответа
generator = RAGGenerator()
context = "\n".join([res['chunk'] for res in results])
answer = generator.generate_answer(context, query)
print(f"Ответ: {answer}")
```

## 🛠️ Основные компоненты

### 1. **SimpleRAGIndexer** (`01_indexing.py`)

Индексатор документов, который:
- 📥 Загружает документы различных форматов (TXT, PDF, DOCX)
- ✂️ Разбивает текст на чанки с перекрытием
- 🧮 Создает векторные эмбеддинги с использованием `all-MiniLM-L6-v2`
- 🗄️ Строит FAISS индекс для быстрого поиска
- 💾 Сохраняет индекс и метаданные для последующего использования

### 2. **SimpleRAGRetriever** (`02_retriever.py`)

Ретривер для поиска релевантной информации:
- 🔍 Поиск по векторному индексу FAISS
- 📊 Ранжирование результатов по схожести
- ⚖️ Фильтрация по порогу схожести
- 🔗 Возврат метаданных (источник, номер чанка)

### 3. **RAGGenerator** (`02_retriever.py`)

Генератор ответов на основе контекста:
- 🤖 Использует модель `facebook/bart-large-cnn`
- 📝 Формирует краткие ответы на основе извлеченной информации
- 🔗 Комбинирует вопрос и контекст для генерации

## ⚙️ Настройка параметров

### Параметры индексации
```python
indexer = SimpleRAGIndexer(
    model_name='all-MiniLM-L6-v2',  # Модель для эмбеддингов
    cache_folder='./model_cache'     # Папка для кэша моделей
)
```

### Параметры разбиения текста
- `chunk_size=500` - размер чанка в символах
- `chunk_overlap=100` - перекрытие между чанками

### Параметры поиска
```python
# Базовый поиск
results = retriever.retrieve(query, top_k=5)

# Поиск с порогом схожести
results = retriever.retrieve_with_threshold(
    query, 
    top_k=10, 
    similarity_threshold=0.7
)
```

### Параметры генерации
```python
generator = RAGGenerator(
    generator_model_name="facebook/bart-large-cnn"  # Модель для генерации
)
```

## 📊 Выходные данные

### После индексации создаются:
- `rag_index.faiss` - бинарный файл FAISS индекса
- `rag_index_data.pkl` - пикл-файл с чанками и метаданными

### Результаты поиска содержат:
```python
{
    'chunk': 'текст фрагмента',
    'metadata': {
        'source': 'имя_файла',
        'chunk_id': 0,
        'total_chunks': 10
    },
    'score': 0.123,        # Расстояние L2
    'similarity': 0.890    # Нормализованная схожесть (0-1)
}
```

## 🔧 Расширение функциональности

### Добавление новых форматов документов
Дополните метод `load_documents` в классе `SimpleRAGIndexer` для поддержки новых форматов.

### Использование других моделей
Замените `model_name` на любую модель из [Sentence Transformers](https://www.sbert.net/docs/pretrained_models.html).

### Настройка разбиения текста
Измените параметры `RecursiveCharacterTextSplitter` для оптимального разбиения под ваш тип документов.

## ⚠️ Ограничения

1. **Объем памяти**: FAISS индекс хранится в оперативной памяти
2. **Размер чанков**: Фиксированный размер может разрезать предложения
3. **Качество эмбеддингов**: Зависит от выбранной модели
4. **Генерация**: Модель BART-Large-CNN ориентирована на суммаризацию

## 📈 Производительность

- ⚡ Быстрый поиск благодаря FAISS
- 🧠 Качественные эмбеддинги от Sentence Transformers
- 🔧 Гибкая настройка параметров
- 💾 Эффективное хранение индекса

## 🤝 Вклад в проект

Приветствуются улучшения и исправления! Пожалуйста:
1. Форкните репозиторий
2. Создайте ветку для новой функциональности
3. Отправьте pull request

## 📄 Лицензия

Этот проект предоставляется "как есть". Используйте на свой страх и риск.

---

**Примечание**: Перед использованием в продакшене протестируйте систему на ваших данных и настройте параметры для оптимальной работы.
